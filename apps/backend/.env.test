# ── Ambiente ───────────────────────────────────────────────
# Opções: dev | test | production
NODE_ENV=test

# ── Servidor ──────────────────────────────────────────────
# Default: 3333
PORT=3334

# ── Segurança ─────────────────────────────────────────────
# Obrigatório — chave secreta para assinatura JWT
JWT_SECRET=test-secret-supersafe-for-vitest

# ── Banco de Dados ────────────────────────────────────────
# Obrigatório — URL de conexão PostgreSQL
# Formato: postgresql://<user>:<pass>@<host>:<port>/<db>
DATABASE_URL=postgresql://mangaink:mangaink@localhost:5432/mangaink_agent_test_db

# ── Cache / Filas ─────────────────────────────────────────
# Default: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# ── Storage ───────────────────────────────────────────────
# Default: ./storage
# Diretório raiz para cache de scraping (sources/) e saída de conversões
STORAGE_PATH=./storage

# Default: ./storage/conversions
# Diretório para conversões (config.json, status.json, output/)
CONVERSIONS_STORAGE_PATH=./storage/conversions

# ── KCC (Kindle Comic Converter) ──────────────────────────
# Default: mangaink-kcc:10.3.0
# Imagem Docker usada pelo kcc-runner.service.ts
KCC_DOCKER_IMAGE=mangaink-kcc:10.3.0

# ── Backend de Persistência ───────────────────────────────
# Opções: filesystem | prisma
# filesystem = JSONs em storage/ (default)
# prisma     = PostgreSQL via tabelas (requer migrations aplicadas)
REPO_BACKEND=prisma

# ── Rate Limiting por Provider (bottleneck) ───────────────
# Valores em milissegundos. Aplica-se a todos os providers
# que não têm config específica.
# Default: 6
# RATE_LIMIT_DEFAULT_MAX_CONCURRENT=6
# Default: 50
# RATE_LIMIT_DEFAULT_MIN_TIME=50

# ── Rate Limiting — Mangá Livre ───────────────────────────
# Default max_concurrent: 8
# RATE_LIMIT_MANGALIVRE_MAX_CONCURRENT=8
# Default min_time: 0
# RATE_LIMIT_MANGALIVRE_MIN_TIME=0
