receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317 # Usado pelo backend Node.js

exporters:
  prometheus:
    endpoint: "0.0.0.0:8889"
  debug:
    verbosity: basic

service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [prometheus, debug]
    traces:
      receivers: [otlp]
      exporters: [debug]
